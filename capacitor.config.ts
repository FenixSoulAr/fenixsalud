import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fenixsoular.myhealthhub',
  appName: 'My Health Hub',
  webDir: 'dist'
};

export default config;
